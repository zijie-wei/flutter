name: Build iOS

on:
  workflow_dispatch:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.38.7'
          channel: 'stable'
          cache: true

      - name: Get dependencies
        run: flutter pub get

      # 构建设备版本（Release）
      - name: Build iOS Device (Release)
        run: flutter build ios --release --no-codesign

      - name: Upload Device .app (Release)
        uses: actions/upload-artifact@v4
        with:
          name: app-device-release-app
          path: build/ios/iphoneos/Runner.app
          retention-days: 30

      # 构建设备版本（Debug）
      - name: Build iOS Device (Debug)
        run: flutter build ios --debug --no-codesign

      - name: Upload Device .app (Debug)
        uses: actions/upload-artifact@v4
        with:
          name: app-device-debug-app
          path: build/ios/iphoneos/Runner.app
          retention-days: 30

      # 构建 ARM 模拟器版本（Release）
      - name: Build iOS ARM Simulator (Release)
        run: flutter build ios --release --no-codesign --simulator

      - name: Upload ARM Simulator .app (Release)
        uses: actions/upload-artifact@v4
        with:
          name: app-arm-simulator-release-app
          path: build/ios/iphonesimulator/Runner.app
          retention-days: 30

      # 构建 ARM 模拟器版本（Debug）
      - name: Build iOS ARM Simulator (Debug)
        run: flutter build ios --debug --no-codesign --simulator

      - name: Upload ARM Simulator .app (Debug)
        uses: actions/upload-artifact@v4
        with:
          name: app-arm-simulator-debug-app
          path: build/ios/iphonesimulator/Runner.app
          retention-days: 30
